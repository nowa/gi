# Pi Compatibility Matrix

This repository is a Go rebuild of the Pi `pi-ai`, `pi-agent-core`, and `pi-tui` packages.

Case-level provider/agent parity is tracked in
[`PI_AI_AGENT_TEST_CASE_PARITY.md`](PI_AI_AGENT_TEST_CASE_PARITY.md). Case-level
TUI parity is tracked in [`PI_TUI_TEST_CASE_PARITY.md`](PI_TUI_TEST_CASE_PARITY.md).

## Package Mapping

| Pi package | Go package | Status |
| --- | --- | --- |
| `@earendil-works/pi-ai` | `github.com/nowa/gi/gi-llm-provider` | Pi test-compatible: core types, model/image-model registry, provider/image-provider registries, event streams, env keys, validation, overflow, message transforms, unicode sanitization, Anthropic, OpenAI Completions/Responses, OpenAI Codex Responses, Azure OpenAI Responses, Google, Mistral, Bedrock, and OpenRouter Images conversion; live HTTP/SSE transport for Anthropic Messages, OpenAI-compatible Chat Completions, OpenAI Responses, OpenAI Codex Responses, Azure OpenAI Responses, Google Generative AI, Mistral Conversations, and OpenRouter Images; faux provider |
| `@earendil-works/pi-agent-core` | `github.com/nowa/gi/gi-agent-core` | Pi test-compatible: agent loop, stateful agent, tools, queues, lifecycle events |
| `pi-agent-core` harness/session | `github.com/nowa/gi/gi-agent-core/harness` | Pi test-compatible: AgentHarness turn orchestration, stream hooks, local execution env, queue lifecycle, skills, prompt templates, system prompt formatting, truncate, uuidv7, storage/session/repo, session compaction, branch tree navigation and branch-summary hook customization |
| `@earendil-works/pi-tui` | `github.com/nowa/gi/gi-tui` | Compatibility layer: Component/Container/TUI/Terminal abstractions including safe ordered child mutation and thread-safe snapshot rendering for chat-style layouts, race-safe mutable render components, overlays including Pi-style integer and decimal percentage `SizeValue` layout, key parsing/keybindings, autocomplete/fuzzy matching, text measurement/wrapping/truncation, reusable KillRing/UndoStack helpers, stdin escape/paste splitting, common components including SettingsList empty-current-value handling and CancellableLoader with Go context plus Pi-style signal/abort compatibility, editor input/undo/paste/autocomplete/rendering, ProcessTerminal raw mode/window sizing through Go's `golang.org/x/term`, keyboard negotiation, and terminal image fallbacks |
| `@earendil-works/pi-coding-agent` | `github.com/nowa/gi/gi-coding-agent` | Initial foundation: standalone coding-agent utility compatibility for CLI arg parsing, prompt-template argument parsing/substitution/loading/expansion, frontmatter parsing/stripping, user path expansion/read-path resolution with macOS filename normalization fallbacks, Pi/chalk-style ANSI stripping, session JSONL file-manager operations including corrupted-file recovery, legacy session migration, custom session IDs, append-only session tree operations for messages, model/thinking changes, custom entries, labels, branch summaries, current-branch context building, session listing, fork-from-session, and branched-session persistence; full agent session runtime, tools, settings, extensions, package manager, CLI command execution, RPC, export, and interactive-mode parity are not ported yet |

## Currently Ported Test Areas

| Pi test file | Go coverage |
| --- | --- |
| `packages/agent/test/agent-loop.test.ts` | `gi-agent-core/agent_loop_test.go` |
| `packages/agent/test/agent.test.ts` | `gi-agent-core/agent_test.go` |
| `packages/agent/test/e2e.test.ts` | `gi-agent-core/agent_e2e_test.go` |
| `packages/agent/test/harness/agent-harness.test.ts` | `gi-agent-core/harness/agent_harness_test.go` |
| `packages/agent/test/harness/agent-harness-stream.test.ts` | `gi-agent-core/harness/agent_harness_test.go` |
| `packages/agent/test/harness/compaction.test.ts` | `gi-agent-core/harness/compaction_test.go` |
| `packages/agent/test/harness/nodejs-env.test.ts` | `gi-agent-core/harness/local_env_test.go` |
| `packages/agent/test/harness/prompt-templates.test.ts` | `gi-agent-core/harness/prompt_templates_test.go` |
| `packages/agent/test/harness/repo.test.ts` | `gi-agent-core/harness/session_repo_test.go` |
| `packages/agent/test/harness/resource-formatting.test.ts` | `gi-agent-core/harness/format_test.go` |
| `packages/agent/test/harness/session-uuid.test.ts` | `gi-agent-core/harness/uuid_test.go` |
| `packages/agent/test/harness/session.test.ts` | `gi-agent-core/harness/session_test.go` |
| `packages/agent/test/harness/skills.test.ts` | `gi-agent-core/harness/skills_test.go` |
| `packages/agent/test/harness/storage.test.ts` | `gi-agent-core/harness/session_storage_test.go` |
| `packages/agent/test/harness/system-prompt.test.ts` | `gi-agent-core/harness/format_test.go` |
| `packages/agent/test/harness/truncate.test.ts` | `gi-agent-core/harness/truncate_test.go` |
| `packages/tui/src/index.ts` | `gi-tui/api_parity_test.go` and `gi-tui/components_test.go` for external-package public API surface coverage across exported constructors, component interfaces, TUI/container child ordering APIs with thread-safe snapshot rendering under concurrent mutation, optional-force `RequestRender` for demo-style redraws, Pi-style TUI getter aliases, overlays with race-safe handle state access and `PercentFloat` decimal percentage sizing, runtime TUI option toggles, keybindings including `TUI_KEYBINDINGS` and Pi `Get*` accessors, keyboard helpers including the Pi-style `Key` builder, stdin buffering, terminal/image helpers including image-ID getters, `WidthPx`/`HeightPx` dimension aliases, Pi-style `ImageRenderOptions` aliases, `RenderImageWithDimensions`, Pi-shaped `ImageFallback`, loader options including Pi-style `IntervalMs`, CancellableLoader `Signal`/abort compatibility, thread-safe idempotent cancellation, `OnAbort` callback behavior, and custom cancel-keybinding behavior, SettingsList Pi empty-state hint behavior and empty `currentValue` choice handling, autocomplete, fuzzy helpers, reusable KillRing/UndoStack helpers, Markdown/Editor options including Pi-shaped editor themes, and utility functions |
| `packages/tui/src/autocomplete.ts` | `gi-tui/autocomplete.go` and `gi-tui/autocomplete_test.go` |
| `packages/tui/src/components/box.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/cancellable-loader.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/editor.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/image.ts` | `gi-tui/components.go` and `gi-tui/terminal_image_test.go` |
| `packages/tui/src/components/input.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/loader.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/markdown.ts` | `gi-tui/components.go`, `gi-tui/markdown_goldmark.go`, and `gi-tui/components_test.go` |
| `packages/tui/src/components/select-list.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/settings-list.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/spacer.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/text.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/components/truncated-text.ts` | `gi-tui/components.go` and `gi-tui/components_test.go` |
| `packages/tui/src/editor-component.ts` | `gi-tui/components.go` and `gi-tui/api_parity_test.go` |
| `packages/tui/src/fuzzy.ts` | `gi-tui/autocomplete.go` and `gi-tui/autocomplete_test.go` |
| `packages/tui/src/keybindings.ts` | `gi-tui/keybindings.go` and `gi-tui/keybindings_test.go` |
| `packages/tui/src/keys.ts` | `gi-tui/keys.go` and `gi-tui/keys_test.go` |
| `packages/tui/src/kill-ring.ts` | `gi-tui/history_test.go` for Pi-style empty push no-op, newest-entry peek, forward/backward accumulation, newest-to-front rotation, and length accessors |
| `packages/tui/src/stdin-buffer.ts` | `gi-tui/stdin_buffer.go`, `gi-tui/stdin_buffer_test.go`, and `gi-tui/terminal_test.go` |
| `packages/tui/src/terminal-image.ts` | `gi-tui/terminal_image.go` and `gi-tui/terminal_image_test.go` |
| `packages/tui/src/terminal.ts` | `gi-tui/terminal.go`, `gi-tui/terminal_native.go`, and `gi-tui/terminal_test.go` |
| `packages/tui/src/tui.ts` | `gi-tui/tui.go`, `gi-tui/tui_test.go`, and `gi-tui/virtual_terminal.go` |
| `packages/tui/src/undo-stack.ts` | `gi-tui/history_test.go` for push/pop/clear/length behavior and clone-on-push semantics via an explicit Go clone function |
| `packages/tui/src/utils.ts` | `gi-tui/utils.go` and `gi-tui/utils_test.go` |
| `packages/tui/test/truncate-to-width.test.ts` | `gi-tui/utils_test.go` for width-bounded truncation of very large unicode input, ANSI style preservation and reset placement around ellipses, malformed ANSI escape prefix handling, wide ellipsis clipping, fitting-text preservation, padded truncation, no-ellipsis reset behavior, contiguous-prefix truncation before wide graphemes, tab/ANSI visible-width accounting, Thai/Lao AM cluster widths, and terminal-output normalization |
| `packages/tui/test/regression-regional-indicator-width.test.ts` | `gi-tui/utils_test.go` for Pi-style grapheme cluster width of regional-indicator flags, emoji modifiers, zero-width joiner emoji, variation selectors, common emoji symbols, keycap emoji clusters, and narrow emoji-presentation bases such as `©️`, `™️`, and arrow+VS16 sequences |
| `packages/tui/test/wrap-ansi.test.ts` | `gi-tui/utils_test.go` for ANSI-preserving, grapheme-aware wrapping/truncation plus Pi-style ANSI extraction including raw and UTF-8 C1 CSI/OSC controls, ESC-ST and BEL OSC 133 visible-width markers, non-greedy OSC parsing, column slicing, segment extraction helpers, colon-form RGB style replay, underline substyle handling without dim leakage, no whitespace before underline reset at wraps, background-color preservation across wrapped lines without full reset, underline close-before-wrap while preserving active background, pure-whitespace width truncation, active SGR/OSC state carryover across literal newlines, underline close-before-wrap without full reset, and OSC 8 hyperlink close/reopen across wrapped lines with BEL/ST terminator preservation without replaying extra OSC 8 sequences outside hyperlinks |
| `packages/tui/test/fuzzy.test.ts` | `gi-tui/autocomplete_test.go` |
| `packages/tui/test/autocomplete.test.ts` | `gi-tui/autocomplete_test.go` for slash commands from `SlashCommand` and simple command items, argument completions, direct path completion including forced root paths, forced absolute paths at line start, forced absolute paths after slash-command arguments, missing/out-of-range cursor line fallback to empty-line forced completion like Pi, Unicode/rune cursor-column ranges and apply behavior, child-provider fallback through `GetSuggestionsContext`, slash-command-name non-trigger behavior, `@` fuzzy file completion with full-path matching, relative-directory scoping, hidden paths excluding `.git`, symlinked files/directories, base-path-independent results, quoted paths, `./` file and directory preservation, Pi-style forced file-completion trigger checks, and apply-completion behavior; Go implementation uses standard-library filesystem traversal rather than external `fd` |
| `packages/tui/test/keybindings.test.ts` | `gi-tui/keybindings_test.go` for Pi-matching default select bindings/descriptions, preserving defaults when keys are reused, reporting direct user-binding conflicts with both Go `Actions` and Pi-style `Keybindings` fields, normalizing duplicate keys, and public getter/export compatibility for `TUI_KEYBINDINGS`, `GetKeys`, `GetDefinition`, `GetResolvedBindings`, `GetUserBindings`, and `GetConflicts` |
| `packages/tui/test/input.test.ts` | `gi-tui/components_test.go` for editing, submit including LF and literal trailing backslashes, escape/cancel callback, `SetValue` cursor clamping, callback execution outside the input mutation lock, narrow prompt rendering, kill/yank ring shortcuts including Ctrl+U/Ctrl+K kill/yank restoration, empty kill-ring yank no-op, single-entry yank-pop no-op, non-delete kill-accumulation breaks, non-yank yank-pop-chain breaks, middle yank/yank-pop placement, forward Ctrl+K kill/yank restoration, and persistent yank-pop rotation, undo coalescing for typing/delete/yank/paste/Alt+D plus empty undo no-op, one-space-at-a-time undo, readline deletions such as Backspace/Ctrl+W/Ctrl+K/Ctrl+U, forward delete including Ctrl+D, Ctrl+B/F cursor movement, cursor movement starting a new undo unit, Alt+D forward word delete, bracketed paste, batched printable text chunks, pi-style horizontal scrolling render with cursor visibility for wide text plus the full Pi CJK/Japanese/Chinese/fullwidth render matrix across start/middle/end cursor positions, grapheme-cluster cursor movement/backspace/delete/fake-cursor rendering, shifted printable CSI-u input including shifted-key fields, race-safe concurrent input mutation/rendering, and ignoring unsupported modified printable key events |
| `packages/tui/test/editor.test.ts` | `gi-tui/components_test.go` for prompt history navigation, duplicate suppression, 100-entry history retention, visual-line history boundaries, history-browsing undo snapshots, history navigation breaking yank-pop state, resolved editor keybindings including Home/End and Alt+B/F overrides, public text/line/cursor accessors, `SetText` normalization/change notification/undo behavior including full-document unicode replacement, runtime padding/autocomplete/max-visible-line option getters/setters with clamping, `EditorComponent` extension interfaces/callback setters, `DisableSubmit`, backslash+Enter newline workaround, Pi newline input fallbacks for LF, `ESC+CR`, and CSI `13;2~`, Shift+Backspace/Delete deletion aliases, basic multiline cursor movement, sticky column across logical/visual line movement including Pi decision-table resize rewrap rebasing, Right-at-final-line capture, and snapped paste-marker pre-snap column preservation, PageUp/PageDown visual page movement, wide emoji/CJK/Thai/Lao rendered-width stability including CJK visual split expectations, unicode text editing for mixed ASCII/umlauts/emoji and newline preservation, grapheme-cluster cursor movement/backspace/delete/cursor highlighting, width-stable wide-character fake cursor rendering, height-aware Pi-style 30%-of-terminal scroll-window sizing via TUI size-aware rendering, pi-style word wrapping with stable source indices, continuous-space/long-whitespace boundary handling, wide-char overflow correction, and oversized atomic segment splitting, pi-style border/fake-cursor/scroll-window rendering including empty editor, exact-fit single-word, continuous-space cases, padding-aware cursor wrap boundary, border-only editor theming without content style leakage, long URL/long-word wrapping without overflow, preserved multiple spaces on rendered lines, no introduced leading whitespace after word wrap, autocomplete list rendering below the editor border with Pi scroll-window/scroll-info padding, and hardware cursor marker suppression while autocomplete is visible, fish-style undo coalescing including newline typing boundaries, submit undo-stack clearing, no-op delete undo boundaries, cursor-movement undo boundaries, kill/yank basics including multiline Ctrl+U/Ctrl+W accumulation, Ctrl+K line-end newline deletion/coalescing, non-delete accumulation breaks, middle yank/yank-pop placement, Alt+D newline deletion, and persistent yank-pop rotation, punctuation-aware word delete/movement including Ctrl+D/B/F, Alt+D forward delete, Ctrl+A insertion at document start, and Ctrl+Left/Right over leading whitespace, Ctrl+] / Ctrl+Alt+] character jump mode including backward multiline jumps, not-found and empty-text no-ops, case-sensitive and special-character targets, repeated-jump/Escape cancellation, and undo coalescing reset after jumps, bracketed paste, programmatic `PasteToEditor`, atomic normalized `InsertTextAtCursor`, large-paste markers, marker expansion on submit, CSI-u paste decoding/printable input including shifted-key fields, batched printable text chunks with autocomplete updates, marker-atomic left/right/delete/word movement, marker-aware visual-line rendering/navigation through paste markers, synchronous autocomplete menu/application/undo behavior, Pi slash-command autocomplete SelectList layout/theme, Enter-to-submit for slash-command name completions, force-file autocomplete narrowing after Tab including single-suggestion auto-apply without opening a menu, slash-argument exact-value retention through both direct providers and `CombinedAutocompleteProvider` model-like command paths, `CombinedAutocompleteProvider` child-provider fallback in the real Editor Tab path, unique prefix-match selection, and first-prefix selection when multiple items match, Pi-style 20ms debounced `@`/`#` autocomplete with actual provider triggering and line-start/space/tab symbol trigger contexts, context-cancelled async suggestions, provider-error autocomplete clearing, autocomplete hiding when slash commands are backspaced to empty, async slash-argument completions, and no argument completions for commands without completers |
| `packages/tui/test/markdown.test.ts` | `gi-tui/components_test.go` for nested list indentation/wrapping including deep unordered, ordered, mixed, unordered marker normalization, parenthesized ordered markers, empty list markers, ordered-list renumbering from list start including inside blockquotes, source-indent stack handling for top-level 0-3-space list markers and nested list markers, lazy list continuation including inside blockquotes, loose list continuation paragraphs after blank lines for unordered, ordered, nested, and blockquote-contained list items, list-item multi-line paragraph token rendering, and multi-digit plus overlong ordered markers, combined heading/list/table rendering, empty text skipping padding, render caching plus explicit `Invalidate`/`SetText` cache clearing, task list marker preservation, ordered numbering around unindented code fences, blockquotes inside list items, blockquotes including lazy continuation, structural/reference-definition termination for lazy continuation, wrapped quote borders, nested blockquote borders, nested list indentation, heading/rule/code-fence/table block rendering, default-text color isolation for styled wrapped blockquotes, spaced thematic-break markers, and quote-style restoration after inline code/bold resets, fenced code including backtick and tilde fences inside list items/blockquotes with opening-indent stripping, indented code blocks at top level, inside blockquotes, inside list items, preserving interior blank lines, and loose list item code blocks including top-level indented list-looking lines as code, code-block highlighter and indent theme hooks, block spacing normalization after paragraphs/headings/code blocks/dividers/blockquotes, trailing blank suppression for terminal blocks, ATX headings including h3 marker preservation, closing-sequence removal, and inline-code heading-style restoration with h1 underline cleanup, Setext headings including multi-line heading tokens inside blockquotes, tables with optional outer pipes, one-column tables, row dividers between data rows, top/bottom borders, varying column widths, natural-fit rendering, GFM delimiter/header column-count validation, multi-column narrow wrapping, alignment markers, marked-style raw pipe splitting inside table rows, raw Markdown fallback for too-narrow tables, longest-token width allocation, narrow width and padding constraints, wrapped long cells/tokens/styled inline code, code spans including multi-backtick delimiters and escaped backticks, basic inline styles including underscore strong/emphasis without intraword matches, nested inline strong/emphasis/strikethrough/code combinations including asterisk strong with nested emphasis, asterisk emphasis with nested strong, and marked-style triple-delimiter strong-emphasis, strict double-tilde strikethrough handling, multi-line paragraph token rendering including cross-line strong/emphasis/strikethrough/code spans/direct links/reference links, markdown direct/reference/shortcut-reference links with Pi-style underline wrapping, empty direct/reference link targets, unsupported-hyperlink URL fallback text without duplicating bare URLs/emails, optional-title handling including quoted parentheses, multi-line and blockquote reference-definition titles, plus Pi-visible non-resolution for list-contained and blockquote-list definitions, balanced-parentheses direct destinations, nested-bracket direct and explicit-reference labels, collapsed/shortcut nested-label fallback rendering, href backslash-unescaping for Markdown punctuation while keeping HTML entities raw, and raw reference-label normalization, generic URI autolinks, email autolinks, CommonMark-style backslash escapes for all ASCII punctuation while preserving non-punctuation escapes, backslash and two-space hard-break marker consumption, raw HTML entity preservation for non-code text tokens, hrefs, and image alt text, direct/reference/shortcut-reference image alt-text fallback including empty targets, optional-title handling with quoted parentheses, balanced-parentheses direct destinations, nested-bracket explicit-reference labels, unresolved collapsed nested labels, and literal raw alt text, mailto links, bare URL/email rendering including case-insensitive HTTP(S)/FTP schemes and marked-style `www.` href normalization, OSC 8 mailto for bare emails, and trailing-punctuation/unmatched-parenthesis exclusion from OSC 8 URL targets while preserving balanced URL parentheses, HTML-like text preservation plus marked-style raw HTML block rendering without parsing inline Markdown inside the block including inside blockquotes and list items, padding/default text style options, default style restoration after inline code/bold resets, and no default-style leakage into following TUI-rendered lines; broader marked-token fuzzing beyond Pi-visible cases is outside the current gate |
| `packages/tui/test/select-list.test.ts` | `gi-tui/components_test.go` for navigation, page navigation, selection callbacks, value-only prefix filtering, multiline description normalization, primary column min/max sizing, truncation hooks, and description alignment |
| `packages/tui/test/keys.test.ts` | `gi-tui/components_test.go` and `gi-tui/keys_test.go` for Kitty base-layout and Super modifiers including non-Latin/Cyrillic alternate-key layouts, Dvorak-style codepoint-over-base preference, shifted/base/event CSI-u forms, negative wrong-key/wrong-modifier checks, Kitty CSI-u digit/event-type/keypad navigation parsing including keypad Enter and arrow/function/home/end repeat-release events, raw and UTF-8 C1 CSI/SS3 equivalents, CapsLock/NumLock modifier masking, xterm modifyOtherKeys including Ctrl/Shift/Alt Enter, Tab, Backspace, Escape, Space, symbols, digits, uppercase letters, and Ctrl+Alt letters, shifted printable input, Pi-style `Key` helper aliases/modifier builders, `DecodePrintableKey`, legacy Ctrl-letter/control bytes/symbols, Windows Terminal backspace ambiguity, expanded SS3/function/rxvt sequences including numpad Enter, legacy alt-arrow variants, both ESC+Ctrl-H / ESC+DEL alt-backspace forms, Kitty-active LF and ESC+CR shift-enter custom mappings, and legacy Alt sequence gating including Kitty-active rejection of generic ESC+character/control Alt forms |
| `packages/tui/test/stdin-buffer.test.ts` | `gi-tui/stdin_buffer_test.go`, `gi-tui/components_test.go`, and `gi-tui/terminal_test.go` for regular/unicode character passthrough, mixed plain-before/after-escape content, multiple complete escape sequences, complete CSI/SS3/meta/SGR-mouse sequence passthrough including raw and UTF-8 C1 CSI/SS3 forms, bracketed paste splitting with chunked arrival, surrounding input, newlines, and unicode, partial escape buffering, explicit flush for lone ESC and incomplete CSI, timeout flush, clear/destroy discard semantics, split and old-style mouse sequences, empty input events including pending-buffer behavior, high-bit byte meta conversion, ESC+ESC+CSI splitting while preserving bare ESC+ESC, Kitty arrow/function event-type sequences, batched Kitty press/release events, rapid Kitty typing, Kitty printable duplicate-character suppression including reset after paste, flush, and unrelated events, callback dispatch outside internal locks, Pi SGR mouse completeness, and OSC/DCS/APC/PM/SOS termination rules including raw and UTF-8 C1 control-string forms |
| `packages/tui/test/terminal.test.ts` | `gi-tui/terminal_test.go` and `gi-tui/terminal_resize_unix_test.go` for dynamic environment dimensions, race-safe explicit size reads/writes, reusable ProcessTerminal start/stop cleanup, Unix/BSD `SIGWINCH` resize callbacks with Stop cleanup, `PI_TUI_WRITE_LOG` output mirroring, Pi public terminal clear-line sequence, Kitty keyboard negotiation including consuming only the initial protocol response, modifyOtherKeys fallback, `drainInput` keyboard cleanup and late-input suppression, startup/stop cleanup sequences including ProcessTerminal bracketed-paste-before-keyboard cleanup and TUI-owned cursor restoration, VirtualTerminal cursor visibility writes and OSC `0`/`2` window-title tracking while preserving OSC 8 visible text, OSC 9;4 progress active/clear keepalive lifecycle, and ProcessTerminal raw mode support on Darwin/Linux/Windows/BSD (`GOOS=linux`, `GOOS=windows`, and BSD compile gates) |
| `packages/tui/test/truncated-text.test.ts` | `gi-tui/components_test.go` for width padding, vertical padding, ANSI preservation, reset-before-ellipsis styled truncation, exact-fit and empty-text padding, first-line-only rendering, and ellipsis truncation before later lines |
| `packages/tui/test/bug-regression-isimageline-startswith-bug.test.ts` | `gi-tui/terminal_image_test.go` for Kitty/iTerm2 image sequence detection at any line position, after ANSI styling, and in long lines |
| `packages/tui/test/overlay-options.test.ts` | `gi-tui/tui_test.go` for default/percentage/min widths including decimal percentage floor semantics, negative-margin clamping, declared-width clipping/padding, max-height clipping including percentage values, short-content placement, anchor positioning across all Pi anchors, margin/offset/absolute/percentage positioning, stacked overlays, edge clipping, synchronized rendering, diff-rendered overlay show/hide/visibility lifecycle without full-screen clears, ANSI/OSC/wide-character overflow handling, ANSI-preserving overlay suffix compositing, and overlay style reset behavior |
| `packages/tui/test/overlay-non-capturing.test.ts` | `gi-tui/tui_test.go` for non-capturing focus preservation, explicit focus/unfocus, hidden-overlay focus behavior, `HideOverlay`/`HasOverlay` behavior with topmost non-capturing overlays, multiple capturing/non-capturing overlay focus restoration through removals, hidden/removed overlay focus no-op guards, nil-preFocus unfocus clearing and input-routing behavior, focus-cycle prevention across non-capturing overlays, invisible focused-overlay input redirection, and focus-order visual stacking |
| `packages/tui/test/overlay-short-content.test.ts` | `gi-tui/tui_test.go` for centered overlays remaining visible when base content is shorter than the terminal viewport |
| `packages/tui/test/tui-overlay-style-leak.test.ts` | `gi-tui/tui_test.go` and `gi-tui/virtual_terminal.go` for ensuring overlay lines keep reset/OSC cleanup after slicing and do not leak italic SGR state into following cells |
| `packages/tui/test/virtual-terminal.ts` | `gi-tui/virtual_terminal.go` and `gi-tui/tui_test.go` for the headless terminal harness API including input injection, resize callbacks, synchronous flush helpers, viewport/scrollback inspection, cursor position inspection, clear/reset, bracketed-paste start/stop hooks, Kitty-protocol-active testing, and DEC save/restore cursor state covering rendition, ISO-2022 charset, origin mode, and wraparound mode |
| `packages/tui/test/chat-simple.ts` | `gi-tui/tui_test.go` for the chat demo composition flow: TUI + Text + Editor + CombinedAutocompleteProvider command items, focus routing, submit handling, dynamic message insertion before the editor, loader insertion/removal, disabled submit while responding, and `/delete` / `/clear` child mutation |
| `packages/tui/test/key-tester.ts` | `gi-tui/tui_test.go` for the key tester demo composition flow: focused custom component input routing, `MatchesKey("ctrl+c")` shutdown, printable/escape/tab key logging with visible representations, max-line log retention, and bracketed-paste cleanup on stop |
| `packages/tui/test/tui-render.test.ts` | `gi-tui/tui_test.go` and `gi-tui/virtual_terminal.go` for synchronized full/diff rendering, Pi-style non-clearing first render on `Start`, `PI_HARDWARE_CURSOR`/`PI_CLEAR_ON_SHRINK` environment defaults, over-wide custom component line rejection before terminal writes, Kitty image cleanup ordering for moved/changed placements, reserved-row redraws, and full redraws, resize full-redraw policy including Termux height-resize suppression, headless viewport rendering, xterm-style soft-wrap reflow on `VirtualTerminal.Resize` while preserving hard line breaks, wide grapheme clusters, streaming keycap/emoji-presentation cluster width expansion, and styled cell attributes, viewport-relative cursor position/DSR reporting after scrollback, viewport-relative CSI screen addressing and edit/scroll line operations after scrollback without hidden scrollback mutation or out-of-range row expansion, viewport-relative overlay composition, TUI invalidation across children and overlays, hardware cursor positioning/visibility updates from `CURSOR_MARKER` including marker disappearance and runtime hardware-cursor toggles, ordered input listeners with explicit empty-data rewrites, key-release opt-in filtering, spinner-style differential updates, low-flicker changed-range-only diff rendering including first-line, last-line, multiple non-adjacent-line, and post-shrink cursor-tracking viewport cases, shrink clearing including empty-state recovery with Pi overlay suppression, viewport reset after shrink, viewport-moving shrink full redraws, changed-above-viewport full redraws, append-after-reset diff rendering, transient high-water cleanup, common CSI cursor/erase/edit/scroll modes (`@`/`C`/`D`/`E`/`F`/`G`/`L`/`M`/`P`/`S`/`T`/`` ` ``/`a`/`b`/`c`/`d`/`e`/`J`/`K`/`X`/`s`/`u`) including xterm-style right-margin clamping for explicit horizontal cursor movement and raw/UTF-8 C1 CSI equivalents, current-style blank cells and wide-cell-aware terminal-column behavior for CSI `@`/`J`/`K`/`P`/`X` erase, insert, and delete operations, current-style blank lines for CSI `L`/`M`/`S`/`T` line operations including scroll regions, viewport-relative scroll regions after scrollback and after resize, CSI `2J` erase-display cursor preservation, CSI `3J` scrollback-only erase, CSI `b` repeat-preceding-character behavior, and primary/secondary device-attributes responses via CSI `c`/`>c`, plus ESC `7`/`8` cursor save/restore, ESC `D`/`E`/`M` index/next-line/reverse-index handling including C1 IND/NEL/RI/HTS equivalents, ISO-2022 charset designation consumption with DEC special graphics mapping through G0/G1, SO/SI, and UTF-8 designation (`ESC % G`/`ESC % @`) consumption, DECALN `ESC # 8`, BEL/ST-terminated OSC/DCS/APC/PM/SOS control-string consumption including raw and UTF-8 C1 string-control forms plus 7-bit string controls terminated by raw/UTF-8 C1 ST, DSR status and cursor-position responses via CSI `5n`/`6n`/`?6n`, DECSTBM scroll regions via CSI `r`, origin-mode addressing via CSI `?6h`/`?6l` including vertical movement clamping within DECSTBM regions, insert/replace mode via CSI `4h`/`4l`, wraparound mode via CSI `?7h`/`?7l`, cursor visibility state via CSI `?25h`/`?25l`, DEC private cursor save/restore via CSI `?1048h`/`?1048l`, alternate-screen restoration via CSI `?47h`/`?1047h`/`?1049h` and matching reset modes, tab-stop control via ESC `H` and CSI `I`/`Z`/`g`, RIS `ESC c` state reset, BS/TAB and xterm-style LF/VT/FF column-preserving line-feed control handling, grapheme-cluster terminal-column writes, xterm-style printable autowrap including wide-rune right-margin wrapping and xterm-style wide-cell overwrite behavior, and SGR bold/dim/italic/underline/inverse/strikethrough/blink/conceal/overline plus ANSI/indexed/RGB foreground/background/underline-color cell-state tracking including semicolon and colon extended-color forms and underline substyle parsing such as CSI `4:2m`; not an xterm-level emulator yet |
| `packages/tui/test/viewport-overwrite-repro.ts` | `gi-tui/tui_test.go` for pure-append diff rendering after content exceeds the viewport, using CRLF scrolling from the current cursor instead of home-plus-absolute-row addressing that can overwrite visible history |
| `packages/tui/test/tui-cell-size-input.test.ts` | `gi-tui/tui_test.go` for startup cell-size query, CSI `6;height;width t` response consumption, cell dimension updates, component invalidation/rerender after size updates, and forwarding unrelated input |
| `packages/tui/test/terminal-image.test.ts` | `gi-tui/terminal_image_test.go` for capability detection across unknown terminals, Ghostty, Kitty, WezTerm, iTerm2, VSCode, tmux, screen, and CMUX-adjacent environments, Kitty/iTerm2 sequence detection at every Pi-tested line position including padding, mixed protocols, repeated image segments, unsupported-terminal detection, ANSI-before/after handling, 300k+ iTerm2 long-line regressions, negative cursor/ANSI/partial/file-path cases, Pi-range random Kitty image IDs, Kitty encoding/delete/delete-all helpers, RenderImage Kitty cursor-movement defaults and suppression plus empty result without an image protocol, Pi-style `ImageRenderOptions` aliases for `imageId`, max cell bounds, and cursor movement, `RenderImageWithDimensions` size calculation, exact OSC 8 hyperlink wrapping including styled text, empty text, empty hrefs, and file URIs, iTerm2 width/height/name/aspect options, format-specific PNG/JPEG/GIF helpers plus generic WebP dimensions, Pi default cell dimensions, thread-safe concurrent capability/cache and cell-dimension global-state access, Pi-style `WidthPx`/`HeightPx` aliases and call-site cell-dimension overrides for cell/image sizing with max-height constraints, Pi-shaped image fallback descriptions through `ImageFallback`, and Image component Kitty/iTerm2 placement with cursor-movement suppression, `ImageID`/`ImageId` reuse semantics, and padding rows |
| `packages/tui/test/image-test.ts` | `gi-tui/terminal_image_test.go` for the manual Image demo flow using TUI + Text + Spacer + Image fallback rendering, Pi-style `ImageTheme.FallbackColor`, parsed dimensions, and non-image terminal fallback layout |
| `packages/tui/test/test-themes.ts` | Shared Pi fixture themes are inlined across `gi-tui/components_test.go`, `gi-tui/tui_test.go`, and `gi-tui/terminal_image_test.go`; there is no standalone runtime behavior in this helper file |
| `packages/coding-agent/test/args.test.ts` | `gi-coding-agent/args_test.go` for Pi-compatible `ParseArgs` flag parsing across help/version/print/continue/resume, scalar flags, mode, sessions, models, thinking levels and diagnostics, resource/theme/skill/prompt-template toggles, tools allowlists, list-models, messages, `@file` arguments, unknown extension flags, and complex CLI combinations |
| `packages/coding-agent/test/prompt-templates.test.ts` | `gi-coding-agent/prompt_templates_test.go` for Pi-compatible command argument parsing, `$1`/`$@`/`$ARGUMENTS`/`${@:N}`/`${@:N:L}` substitution without recursive argument expansion, newline-separated template arguments, non-template fallback, prompt directory/file loading, frontmatter descriptions, `argument-hint`, default user/project prompt directories, ignored missing paths, and non-Markdown filtering |
| `packages/coding-agent/test/frontmatter.test.ts` | `gi-coding-agent/utils_test.go` for quoted scalar parsing, CRLF normalization, pipe-style multiline values, invalid YAML diagnostics, missing/unterminated frontmatter fallback, comment-only metadata, and `StripFrontmatter` body trimming |
| `packages/coding-agent/test/path-utils.test.ts` | `gi-coding-agent/utils_test.go` for `~` expansion, Unicode no-break-space normalization, cwd-relative resolution, existing-file resolution, NFC/NFD accent fallback, straight/curly quote fallback, and macOS screenshot AM/PM narrow no-break-space variants |
| `packages/coding-agent/test/ansi-utils.test.ts` | `gi-coding-agent/utils_test.go` for Pi/chalk-compatible stripping of SGR, OSC 8 hyperlinks, unterminated OSC fallback behavior, C1 CSI, RIS, selected single-byte ESC sequences, and common tool-output ANSI sequences |
| `packages/coding-agent/test/session-manager/file-operations.test.ts` | `gi-coding-agent/session_manager_test.go` for Pi-compatible JSONL entry loading with malformed-line skipping and session-header validation, most-recent valid `.jsonl` session selection by mtime, `SessionManager` open/create/continue entry points, default encoded session-directory creation, explicit-path preservation, and corrupted empty/no-header/garbage session-file recovery |
| `packages/coding-agent/test/session-manager/tree-traversal.test.ts` and `packages/coding-agent/test/session-manager/save-entry.test.ts` | `gi-coding-agent/session_manager_tree_test.go` for append-only parentId chains, leaf tracking, branch navigation, tree construction, custom-entry persistence in the tree, current-branch session context filtering, branch summaries as context messages, missing-entry errors, and branched-session persistence with deferred writes before the first assistant response |
| `packages/coding-agent/test/session-manager/migration.test.ts`, `labels.test.ts`, `custom-session-id.test.ts`, and session listing/fork coverage in `core/session-manager.ts` | `gi-coding-agent/session_manager_migration_list_test.go` for v1/v2 migration to current session format, hook-message role migration, open-time rewrite of legacy files, custom and generated UUIDv7 session IDs, label set/clear/last-wins behavior, label timestamps in trees, reset-leaf empty context, branch label preservation and off-path label pruning, session list metadata/progress/sort behavior, and fork-from-session headers/history |
| `packages/ai/test/env-api-keys.test.ts` | `gi-llm-provider/env_test.go` |
| `packages/ai/test/faux-provider.test.ts` | `gi-llm-provider/faux_test.go` |
| `packages/ai/test/fireworks-models.test.ts` | `gi-llm-provider/model_catalog_test.go` |
| `packages/ai/test/images.test.ts` | `gi-llm-provider/openrouter_images_test.go` |
| `packages/ai/test/anthropic-eager-tool-input-compat.test.ts` | `gi-llm-provider/anthropic_payload_test.go` |
| `packages/ai/test/anthropic-eager-tool-input-e2e.test.ts` | `gi-llm-provider/anthropic_e2e_contracts_test.go` |
| `packages/ai/test/anthropic-long-cache-retention-e2e.test.ts` | `gi-llm-provider/anthropic_e2e_contracts_test.go` |
| `packages/ai/test/anthropic-oauth.test.ts` | `gi-llm-provider/oauth_test.go` |
| `packages/ai/test/anthropic-opus-4-7-smoke.test.ts` | `gi-llm-provider/anthropic_e2e_contracts_test.go` |
| `packages/ai/test/anthropic-sse-parsing.test.ts` | `gi-llm-provider/anthropic_stream_test.go` |
| `packages/ai/test/anthropic-thinking-disable.test.ts` | `gi-llm-provider/anthropic_payload_test.go` |
| `packages/ai/test/anthropic-tool-name-normalization.test.ts` | `gi-llm-provider/anthropic_payload_test.go` |
| `packages/ai/test/abort.test.ts` | `gi-llm-provider/event_stream_test.go` |
| `packages/ai/test/azure-openai-base-url.test.ts` | `gi-llm-provider/config_test.go` |
| `packages/ai/test/bedrock-endpoint-resolution.test.ts` | `gi-llm-provider/config_test.go` |
| `packages/ai/test/bedrock-models.test.ts` | `gi-llm-provider/model_catalog_test.go` |
| `packages/ai/test/cache-retention.test.ts` | `gi-llm-provider/anthropic_payload_test.go`, `gi-llm-provider/openai_completions_payload_test.go` |
| `packages/ai/test/context-overflow.test.ts` | `gi-llm-provider/overflow_test.go` |
| `packages/ai/test/cross-provider-handoff.test.ts` | `gi-llm-provider/cross_provider_handoff_test.go` |
| `packages/ai/test/empty.test.ts` | `gi-llm-provider/provider_contracts_test.go` |
| `packages/ai/test/google-shared-convert-tools.test.ts` | `gi-llm-provider/google_convert_test.go` |
| `packages/ai/test/google-shared-gemini3-unsigned-tool-call.test.ts` | `gi-llm-provider/google_convert_test.go` |
| `packages/ai/test/google-shared-image-tool-result-routing.test.ts` | `gi-llm-provider/google_convert_test.go` |
| `packages/ai/test/google-thinking-disable.test.ts` | `gi-llm-provider/google_convert_test.go` |
| `packages/ai/test/google-thinking-signature.test.ts` | `gi-llm-provider/google_convert_test.go` |
| `packages/ai/test/google-vertex-api-key-resolution.test.ts` | `gi-llm-provider/config_test.go` |
| `packages/ai/test/github-copilot-anthropic.test.ts` | `gi-llm-provider/github_copilot_headers_test.go` |
| `packages/ai/test/github-copilot-oauth.test.ts` | `gi-llm-provider/oauth_test.go` |
| `packages/ai/test/image-tool-result.test.ts` | `gi-llm-provider/anthropic_payload_test.go`, `gi-llm-provider/google_convert_test.go`, `gi-llm-provider/openai_completions_convert_test.go`, `gi-llm-provider/openai_responses_convert_test.go` |
| `packages/ai/test/interleaved-thinking.test.ts` | `gi-llm-provider/anthropic_payload_test.go`, `gi-llm-provider/bedrock_payload_test.go` |
| `packages/ai/test/bedrock-thinking-payload.test.ts` | `gi-llm-provider/bedrock_payload_test.go` |
| `packages/ai/test/mistral-reasoning-mode.test.ts` | `gi-llm-provider/mistral_payload_test.go` |
| `packages/ai/test/mistral-tool-schema.test.ts` | `gi-llm-provider/mistral_payload_test.go` |
| `packages/ai/test/node-http-proxy.test.ts` | `gi-llm-provider/config_test.go` |
| `packages/ai/test/openai-completions-cache-control-format.test.ts` | `gi-llm-provider/openai_completions_payload_test.go` |
| `packages/ai/test/openai-completions-empty-tools.test.ts` | `gi-llm-provider/openai_completions_convert_test.go` |
| `packages/ai/test/openai-completions-prompt-cache.test.ts` | `gi-llm-provider/openai_completions_payload_test.go` |
| `packages/ai/test/openai-completions-response-model.test.ts` | `gi-llm-provider/openai_completions_stream_test.go` |
| `packages/ai/test/openai-completions-thinking-as-text.test.ts` | `gi-llm-provider/openai_completions_payload_test.go` |
| `packages/ai/test/openai-completions-tool-result-images.test.ts` | `gi-llm-provider/openai_completions_convert_test.go` |
| `packages/ai/test/openai-completions-tool-choice.test.ts` | `gi-llm-provider/openai_completions_payload_test.go` |
| `packages/ai/test/openai-codex-oauth.test.ts` | `gi-llm-provider/oauth_test.go` |
| `packages/ai/test/openai-codex-cache-affinity-e2e.test.ts` | `gi-llm-provider/openai_codex_test.go` |
| `packages/ai/test/openai-codex-stream.test.ts` | `gi-llm-provider/openai_codex_test.go`, `gi-llm-provider/openai_responses_stream_test.go` |
| `packages/ai/test/openai-responses-copilot-provider.test.ts` | `gi-llm-provider/openai_responses_payload_test.go`, `gi-llm-provider/openai_responses_stream_test.go` |
| `packages/ai/test/openai-responses-cache-affinity-e2e.test.ts` | `gi-llm-provider/openai_responses_payload_test.go` |
| `packages/ai/test/openai-responses-foreign-toolcall-id.test.ts` | `gi-llm-provider/openai_responses_convert_test.go` |
| `packages/ai/test/openai-responses-partial-json-cleanup.test.ts` | `gi-llm-provider/openai_responses_stream_test.go` |
| `packages/ai/test/openai-responses-reasoning-replay-e2e.test.ts` | `gi-llm-provider/openai_responses_replay_test.go` |
| `packages/ai/test/openai-responses-tool-result-images.test.ts` | `gi-llm-provider/openai_responses_stream_test.go` |
| `packages/ai/test/openrouter-images.test.ts` | `gi-llm-provider/openrouter_images_test.go` |
| `packages/ai/test/openrouter-cache-write-repro.test.ts` | `gi-llm-provider/openai_completions_stream_test.go` |
| `packages/ai/test/responseid.test.ts` | `gi-llm-provider/openai_responses_stream_test.go` |
| `packages/ai/test/stream.test.ts` | `gi-llm-provider/stream_contract_test.go`, `gi-llm-provider/faux_test.go` |
| `packages/ai/test/tool-call-id-normalization.test.ts` | `gi-llm-provider/message_transform_test.go` |
| `packages/ai/test/tool-call-without-result.test.ts` | `gi-llm-provider/provider_contracts_test.go`, `gi-llm-provider/message_transform_test.go` |
| `packages/ai/test/total-tokens.test.ts` | `gi-llm-provider/provider_contracts_test.go` |
| `packages/ai/test/tokens.test.ts` | `gi-llm-provider/abort_usage_test.go`, `gi-llm-provider/event_stream_test.go` |
| `packages/ai/test/together-models.test.ts` | `gi-llm-provider/model_catalog_test.go` |
| `packages/ai/test/transform-messages-copilot-openai-to-anthropic.test.ts` | `gi-llm-provider/message_transform_test.go` |
| `packages/ai/test/unicode-surrogate.test.ts` | `gi-llm-provider/message_transform_test.go` |
| `packages/ai/test/overflow.test.ts` | `gi-llm-provider/overflow_test.go` |
| `packages/ai/test/supports-xhigh.test.ts` | `gi-llm-provider/models_test.go` |
| `packages/ai/test/xhigh.test.ts` | `gi-llm-provider/models_test.go` |
| `packages/ai/test/validation.test.ts` | `gi-llm-provider/validation_test.go` |
| `packages/ai/test/zen.test.ts` | `gi-llm-provider/model_catalog_test.go` |
| `packages/ai/test/lazy-module-load.test.ts` | N/A in Go: the package has no provider SDK module dependencies; providers are explicit registry entries |

## Additional TUI Coverage Notes

- Combined autocomplete applies slash-command argument completions against the current full argument, so stale async menus cannot overwrite an exact typed argument with a shorter prefix match.
- ProcessTerminal now matches Pi's Kitty keyboard negotiation boundary: the first `CSI ? ... u` response is consumed to enable protocol flags, while later responses are forwarded as normal input instead of being repeatedly swallowed.
- ProcessTerminal Stop now follows Pi cleanup ownership: bracketed paste is disabled before keyboard protocols, and cursor restoration remains owned by `TUI.Stop`.
- StdinBuffer now collects parsed data/paste events under lock and dispatches callbacks after unlocking, preserving Pi's EventEmitter-style callback boundary while avoiding Go callback deadlocks.
- StdinBuffer now has a Pi-derived behavior matrix for ordinary rune splitting, partial escape buffering, split SGR/old-style mouse events, C1/OSC/DCS/APC/PM/SOS control-string framing, ESC+ESC+CSI handling, Kitty printable duplicate suppression, bracketed paste chunking, high-byte meta conversion, flush, clear, and destroy timeout cleanup.
- Input editing now uses resolved keybindings like Pi for submit/cancel, cursor movement, deletion, kill/yank, and undo, so custom user bindings replace default shortcuts.
- Input keybinding priority now follows Pi's action order when custom bindings overlap default keys, including undo-before-submit and delete-before-line-movement behavior.
- Editor character-jump cancellation now falls through to normal handling for non-printable control keys like Pi, so Backspace/Delete still edit after leaving jump mode.
- Editor now consumes the resolved `tui.input.copy` keybinding as a no-op like Pi before normal editing actions.
- Editor keybinding priority now follows Pi's action order when custom bindings overlap default keys, including delete-before-newline and delete-to-line-end-before-submit behavior.
- Loader instances constructed with an attached `TUI` now auto-start their frame timer like Pi's constructor while still avoiding a goroutine for detached loaders.
- ProcessTerminal now reads the live terminal window size from stdout on Unix/BSD/macOS and Windows before falling back to `COLUMNS`/`LINES`, matching Pi's actual-terminal-first sizing behavior.
- ProcessTerminal raw mode, restore, TTY detection, and live terminal size now use Go's official `golang.org/x/term` package instead of per-platform hand-written syscalls, while preserving no-op behavior for non-TTY test readers.
- On Windows, `golang.org/x/term.MakeRaw` already enables `ENABLE_VIRTUAL_TERMINAL_INPUT`, so the Go implementation preserves Pi's modified-key input requirement without a separate Koffi-style syscall layer.
- Markdown input normalizes each tab to three spaces before block and inline parsing like Pi, including code-span text, custom HTML block tag attributes, and the one-tab vs space-plus-tab indented-code boundary.
- Markdown generic URI autolinks now follow marked's scheme rule, including two-or-more-character schemes with an empty opaque part such as `<aa:>` while keeping one-character schemes like `<x:>` literal.
- Markdown email autolinks now use marked's broader local-part punctuation rules for angle-bracket autolinks, including addresses such as `<a!b@example.com>` and `<o'hara@example.com>`.
- Markdown bare email links now follow marked's domain-label and `-`/`_` suffix backtracking, including one-character final labels such as `user@example.c`.
- Markdown list markers now apply `MarkdownTheme.ListBullet`, including task-list checkbox markers, and ordered lists only interrupt an existing paragraph from marker `1.` like marked/CommonMark.
- Markdown ATX headings accept marked-style empty headings and tab-separated heading text while still preserving literal overlong marker lines.
- Markdown Setext heading underlines now follow marked/CommonMark indentation limits: up to 3 leading spaces are accepted, while 4-space indented marker lines remain literal/code content.
- Markdown list-contained blockquotes now preserve Pi's first-line vs continuation prefixes across explicit multi-line `>` quote bodies, including blockquotes nested inside outer blockquote list items.
- Markdown GFM tables now follow marked/CommonMark indentation and delimiter limits: up to 3 leading spaces are accepted, 4-space table-looking input remains code/non-table content, empty header/data cells are preserved, malformed multi-colon alignment markers are rejected, alignment markers render left-aligned like Pi, and narrow table column widths use Pi's weighted longest-token allocation with the same 30-column cap.
- Markdown GFM tables now use marked-style raw pipe splitting for table rows, so code spans and escaped pipes do not protect cell boundaries; list-contained tables render with the list marker on the first table border and continuation indentation on remaining rows.
- Markdown top-level code block wrapping now mirrors Pi's visible layout: the code line receives the configured indent before wrapping, but continuation lines are not re-indented; list-contained code blocks keep their Pi list continuation indentation.
- Markdown raw HTML block detection now includes marked-style standalone custom tags such as `<thinking>` while preserving inline HTML-like text behavior.
- Markdown inline HTML tags and comments now stay raw like marked tokens, including marked tag-name rules for `_` in opening tags and `:` in closing tags, while invalid opening tags and declarations fall back to normal Markdown text parsing instead of being over-stashed as raw HTML.
- Markdown emphasis and strong-emphasis delimiter matching now follows marked/CommonMark left/right flanking around punctuation, Unicode symbols, the GFM tilde exception, and intraword underscores, so quoted, parenthesized, or symbol-bound text gets the same styling decisions as marked.
- Markdown mixed emphasis delimiter runs now split like marked/CommonMark for both `*` and `_`, including triple-open cases such as `***foo** bar*` / `___foo__ bar_`, triple-close cases such as `*foo **bar***` / `_foo __bar___`, 4/5-asterisk nesting such as `****four****` and `*****five*****`, and leftover delimiter cases such as `*em**`, `**bold*`, `***both**`, `__foo___bar_`, `_foo___bar__`, and `**\**not open?**`.
- Markdown strict strikethrough now follows Pi's custom marked tokenizer for escaped closing-boundary content, so `~~trail\ ~~` and escaped boundary tildes are styled while ordinary single-tilde or space-bounded spans stay literal.
- Markdown nested inline tokens inside direct/reference link labels now fully restore stashed code spans, inline HTML, and image alt fallbacks before rendering the link, avoiding placeholder leakage in marked-style labels.
- Markdown nested link tokens inside direct and explicit-reference link labels now render like marked, while collapsed/shortcut labels with nested brackets stay literal and can still render inner shortcut links.
- Markdown link-label rendering now follows marked's in-link tokenization for escaped closing brackets and bare URLs: escaped `\]` can keep an inner link candidate literal, bare URLs/emails inside labels remain text, and angle autolinks still render as nested links.
- Markdown escaped link/image prefixes now follow marked tokenization: `\[` and `\!` escape only the prefix byte, while following bare URLs or `[label](...)` / `[label][ref]` link tokens still render as links instead of being stashed as whole literal links/images.
- Markdown explicit trailing space tokens now stay visible like Pi, including paragraph input ending with blank lines and paragraphs followed by hidden reference definitions.
- Markdown CommonMark type-1 raw HTML blocks now recognize `pre`/`script`/`style`/`textarea` starts, including end-of-line start tags and `textarea` blocks containing blank lines inside list and blockquote-list containers.
- Markdown raw HTML blocks now cover marked/CommonMark comment, processing-instruction, valid declaration, and CDATA block tokens, including list-contained and blockquote-list-contained comment blocks, while invalid declarations fall back to paragraph Markdown instead of over-stashing raw text.
- Markdown custom HTML block tags now follow marked/CommonMark type-7 paragraph behavior, tag-name rules, and attribute validation, including `_` custom tags as raw blocks, invalid custom tags as Markdown paragraphs, and `:` opening tags as paragraph text; custom tags remain paragraph text when not separated by a blank line, while standard HTML block tags such as `<div>` still interrupt paragraphs as raw blocks.
- Markdown standard HTML blocks now stop lazy blockquote/list continuation like marked and reject malformed starts such as `<div/abc>`, so raw HTML outside the quote/list is not swallowed into the previous paragraph or parsed as inline Markdown.
- Markdown list-continuation HTML blocks now keep Pi's list and blockquote-list continuation prefixes instead of falling back to top-level raw HTML indentation.
- Markdown blockquote raw HTML blocks now apply quote styling across the whole raw block like Pi's marked token renderer, rather than restyling each HTML line independently.
- Markdown blockquote-contained list rows now apply quote styling to the whole rendered list line, including bullets and continuation prefixes, matching Pi's render-then-style order.
- Markdown inline rendering now preserves Pi's base-style restoration suffix after styled tokens at line ends, so nested link/code emphasis inside quoted text produces the same terminal style boundary.
- Markdown loose list blank lines now follow marked's visible spacing: blank lines between items in one loose list do not add rendered spacer rows, while reference-like list continuations keep Pi's spacer before a nested list.
- Markdown direct links and reference definitions now reject unquoted optional-title text and unescaped title delimiters like marked, while still accepting escaped delimiters inside `"..."`, `'...'`, and `(...)` titles without swallowing later valid links.
- Markdown text, image alt text, inline HTML text, autolink labels, and hrefs now keep HTML entities raw like Pi's marked-token renderer; image alt backslash escapes stay literal, named entity suffixes such as `&copy;` remain visible outside bare URL links, while query strings such as `&amp;b=2` stay raw inside labels and OSC 8 hrefs.
- Markdown reference definitions now follow marked block boundaries more closely: they do not interrupt existing paragraphs, list-contained and blockquote-list-contained definitions remain visible instead of resolving later links, and nested-bracket collapsed/shortcut definitions render as paragraph text.
- Markdown list and blockquote-list continuation lines following visible reference-definition-like text now remain paragraph text like Pi instead of being promoted to indented code blocks.
- Markdown GFM table delimiters now accept marked's single-dash cells such as `| - | - |` instead of requiring three dashes.
- Markdown angle autolinks and bare URL/email tokens are matched before Markdown backslash escape stashing, so escaped punctuation inside URLs such as `<http://example.com/a\*b>` or `www.example.com/a\*b` stays raw like marked.
- Markdown duplicate reference definitions now keep the first normalized top-level or blockquote definition like marked; list-contained definitions are not part of the definition map.
- Markdown multi-line reference definitions are consumed for top-level and blockquote definitions, including labels split across lines and destination/title continuations after the colon; list-contained and blockquote-list-contained multiline definitions remain visible like Pi.
- Markdown rendering now builds an internal GFM-enabled `goldmark` AST/context document before the existing Pi terminal renderer runs, including task-list, table, and strikethrough nodes, while keeping the marked-compatible reference-definition shim authoritative until the full AST renderer migration is complete.
- Markdown definition-only non-empty input now renders Pi's single blank line (or vertical padding only) instead of disappearing as empty text.
- Markdown Kitty/iTerm image protocol lines bypass wrapping, horizontal padding, and background styling like Pi so terminal image control sequences are not split or corrupted.
- Terminal image dimension helpers now have Pi-format coverage for JPEG and GIF in addition to PNG/WebP, including generic dimension detection and format-specific rejection of mismatched image data.
- VirtualTerminal CSI cursor, tab, and scroll-region movement now treats explicit zero parameters like xterm defaults for `CUU`/`CUD`/`CUF`/`CUB`/`CNL`/`CPL`/`HPR`/`VPR`/`CHT`/`CBT` and `DECSTBM`, so `CSI 0A` and `CSI 0;0r` follow default movement/region semantics rather than becoming no-ops.
- VirtualTerminal tab-stop control now has Pi harness coverage for clearing the current default stop with `CSI g`, clearing all stops with `CSI 3g`, setting explicit stops after clearing defaults, and restoring default stops through `RIS`.
- VirtualTerminal CSI edit/scroll counts now treat explicit zero parameters like xterm defaults for `ICH`/`DCH`/`ECH`/`IL`/`DL`/`SU`/`SD`, so `CSI 0@`, `CSI 0P`, `CSI 0X`, `CSI 0L`, `CSI 0M`, `CSI 0S`, and `CSI 0T` perform one operation rather than no-oping.
- VirtualTerminal Backspace and LF/VT/FF now follow xterm pending-wrap behavior at the right margin: after writing the final column, `BS` cancels the pending wrap and moves to the previous visible cell, while line-feed controls preserve the last real column instead of the virtual post-wrap column.
- VirtualTerminal wraparound mode now preserves xterm's pending right-margin state across `CSI ? 7 h/l`, so a printable written after re-enabling wraparound moves to the next line instead of overwriting the final cell.
- VirtualTerminal now handles xterm/DEC `DECSTR` soft reset (`CSI ! p`) by resetting active modes, rendition, scroll region, charsets, and saved cursor state without clearing visible text or moving the active cursor.
- VirtualTerminal viewport and scroll-buffer reads now trim trailing blank cells like Pi's xterm harness (`translateToString(true)`), while `GetCell` still exposes styled blank cells; `CSI @`, `CSI X`, and `CSI 1K`/`CSI 1J` past-text operations preserve current-cell styling without leaking trailing blanks into viewport strings.
- VirtualTerminal SGR style tracking now treats `CSI 21m` as xterm-style double underline without clearing bold; `CSI 22m` remains the bold/dim reset, and colon underline substyles such as `CSI 4:2m` are exposed through `VirtualCell.UnderlineStyle`.
- VirtualTerminal `Clear()` now follows the Pi `@xterm/headless` harness API: it clears scrollback/viewport while keeping the active prompt line as the new first line and preserving the cursor column on that line, instead of behaving like a full terminal reset.
- VirtualTerminal alternate-screen entry now preserves the current SGR rendition for writes in the alternate buffer, while `CSI ? 1049 l` restores the saved normal-screen cursor/rendition state like xterm.
- An isolated `github.com/gitpod-io/xterm-go` adapter spike matched the current 32-case Pi `@xterm/headless` audit matrix with zero diff, but it remains an untagged pseudo-version dependency, so `gi-tui` keeps the deterministic in-repo emulator for now.
- TUI overlay focus-order coverage now includes Pi's exact visual stacking matrix for refocusing an already-focused overlay above newer overlays, raising a middle overlay while preserving the remaining overlay order, and restoring a hidden capturing overlay to the top when it becomes visible again.

## Outside Current Test Gate

- Credentialed live probes are intentionally not part of the local Go suite.
- Provider-specific live transports not exercised by the Pi tests should be validated before production use.
- Remote filesystems, sandbox service adapters, and WebSocket transports are extension points rather than requirements for the current `pi-agent-core` / `pi-ai` test compatibility gate.
- Future `gi-tui` hardening beyond the current Pi TUI use-case gate should continue by larger surface rather than edge-case trickle: broader Markdown token/style fuzzing, xterm-level virtual-terminal gaps beyond the current audit matrix, and raw terminal fallbacks for platforms outside `golang.org/x/term`.
- Markdown parser parity has started moving toward a `goldmark` AST plus custom Pi terminal renderer rather than indefinitely expanding the hand-written Markdown lexer; keep Pi visible-output fixtures as the acceptance gate because Pi uses `marked`, not `goldmark`.
- VirtualTerminal remains scoped to Pi TUI behavior. The `xterm-go` adapter spike has been evaluated against the existing Pi-derived harness tests and can be revisited once the dependency has stable tags or when a larger xterm behavior gap appears.
- `gi-coding-agent` parity remains tracked separately and is not part of the current TUI completion scope.

## Local Verification

Run:

```sh
GOCACHE=/private/tmp/gi-gocache go test -timeout 30s ./...
GOCACHE=/private/tmp/gi-gocache go test -race -timeout 90s ./...
GOCACHE=/private/tmp/gi-gocache go vet ./...
GOOS=linux GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-linux.test
GOOS=windows GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-windows.test.exe
GOOS=freebsd GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-freebsd.test
GOOS=openbsd GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-openbsd.test
GOOS=netbsd GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-netbsd.test
GOOS=dragonfly GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-tui -o /private/tmp/gi-tui-dragonfly.test
GOOS=linux GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-coding-agent -o /private/tmp/gi-coding-agent-linux.test
GOOS=windows GOARCH=amd64 GOCACHE=/private/tmp/gi-gocache go test -c ./gi-coding-agent -o /private/tmp/gi-coding-agent-windows.test.exe
```

Current local top-level Go test list count: 672.
